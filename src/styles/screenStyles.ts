import { StyleSheet } from 'react-native';

const screenStyles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 24,
  },
  subtitle: {
    marginBottom: 24,
    marginTop: 8,
    opacity: 0.7,
  },
});

export { screenStyles };
